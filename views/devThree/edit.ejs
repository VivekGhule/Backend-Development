<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> Users Database</title>
  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
    <a class="mb-8 text-blue-500" href="/read">Read Users</a>
    <br>
    <br>
   <form action="/edit/<%= user._id%>" method="post">
     <input 
      class="px-4 py-2 border mr-5 border-zinc-500 rounded bg-zinc-800 text-white focus:outline-none focus:border-blue-500"  
      type="text" 
      placeholder="Name" 
      name="name"
      value="<%= user.name %>"
    >
    <input 
      class="px-4 py-2 border mr-5  border-zinc-500 rounded bg-zinc-800 text-white focus:outline-none focus:border-blue-500"  
      type="text" 
      placeholder="Email" 
      name="email"
      value="<%= user.email %>"
    >
    <input 
      class="px-4 py-2 border mr-5 border-zinc-500 rounded bg-zinc-800 text-white focus:outline-none focus:border-blue-500"  
      type="text" 
      placeholder="Url" 
      name="url"
      value="<%= user.url %>"
    >
    <input 
      class="px-8 py-2 border  border-zinc-500 rounded bg-yellow-500 text-white focus:outline-none focus:border-blue-500"  
      type="submit" 
      name="submit"
    >
   </form>

  </div>
</body>
</html>
