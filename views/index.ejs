<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes Taking Web App</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900">
        <div class="form ">
           <form class="flex flex-col text-white p-10" action="/create" method="post">
             <input  class=" outline-none w-full bg-zinc-800 mt-5 px-3 py-3 rounded-md" name="title" type="text" placeholder="Write title here...">
            <textarea class=" outline-none w-full bg-zinc-800 mt-5 px-3 py-3 rounded-md" name="desc" placeholder="write Details here..."></textarea>
            <input class="px-4 py-2 bg-blue-500 w-40 mt-5 rounded-md"  type="submit">
           </form>
        </div>

        <div class="task-container p-10 text-white flex gap-5">

            <% if (files.length > 0) { %>
                <% files.forEach(function (val) {  %>

                    <div class="task w-80  bg-zinc-800 p-4 rounded-md">
                        <h1 class="text-2xl"><%= val %></h1>
                       <div class="flex justify-between mt-3 "> 
                        <a class="inline-block text-blue-500" href="/files/<%= val %>">Read More</a>
                        <a class="inline-block text-zinc-500" href="/<%= val %>">edit filename</a></div>
                    </div> 
                    
              <%  }) %>
                
                
           <% } else{ %>
            
            <h2 class="text-zinc-500">No task yet</h2>
           <% } %>
                



           
            
        </div>

    </div>
</body>
</html>